// Test stub for expo-constants (RN-only). In Node unit tests __DEV__ is undefined,
// so resolveDevHost returns the configured URL unchanged and this is never read;
// present so the import resolves.
module.exports = { default: { expoConfig: null } };
